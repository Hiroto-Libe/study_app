小4向け学習アプリ詳細設計書v1.1（2026年4月 ― ChatGPT / Gemini レビュー反映版）項目内容ベース文書仕様検討書 v1.1対象フェーズPhase 1（コア学習機能）v1.1 変更概要ChatGPT・Gemini レビュー全20件反映。Supabaseクライアント分離・クエリバグ修正・トランザクション安全化・初期取得ロジック修正・XP計算バグ修正・型安全強化・エラーハンドリング強化0. 改訂履歴版日付主な変更内容v1.02026/04/12初版作成v1.12026/04/12【高】Supabaseクライアント3分割・maybeSingle修正・study/answerシリアル化・逐次取得・calcNextLevelXpバグ修正・StrokeAnimation正解Index修正 ／ 【中】Zod方針・mapFormatToWeakType・prev_stage追加・JSON解析堅牢化・DOMParser化・errMap完全化・kanji=null空文字化・res.okチェック・error.tsx追加・idempotent seed ／ 【低】payload明示・rate limit注記【v1.1修正】 が付いた箇所が今回の変更点です。1. プロジェクト構成1.1 ディレクトリ構成（v1.1修正版）my-study-app/├── app/│   ├── layout.tsx│   ├── globals.css│   ├── learn/│   │   ├── layout.tsx│   │   ├── error.tsx          ← 【v1.1追加】Next.js App Router エラーUI│   │   ├── loading.tsx        ← 【v1.1追加】ローディングUI│   │   ├── page.tsx│   │   ├── quiz/voice/page.tsx│   │   ├── quiz/stroke/page.tsx│   │   ├── quiz/number/page.tsx│   │   ├── quiz/choice/page.tsx│   │   ├── result/page.tsx│   │   └── character/page.tsx│   ├── parent/│   │   ├── layout.tsx│   │   ├── error.tsx          ← 【v1.1追加】│   │   ├── loading.tsx        ← 【v1.1追加】│   │   ├── page.tsx│   │   └── settings/page.tsx│   └── api/│       ├── questions/next/route.ts│       ├── study/answer/route.ts│       ├── game/progress/route.ts│       └── parent/dashboard/route.ts├── components/ （変更なし）├── lib/│   ├── supabase.ts            ← 【v1.1修正】3クライアント分離│   ├── claude.ts              ← 【v1.1修正】JSON解析堅牢化・validate強化│   ├── kanjivg.ts             ← 【v1.1修正】DOMParser化│   ├── kanjivg-node.ts        ← 【v1.1追加】checkCoverage（Node専用）│   ├── speech.ts              ← 【v1.1修正】errMap完全化│   ├── xp.ts                  ← 【v1.1修正】calcNextLevelXp バグ修正│   └── priority.ts            ← 【v1.1修正】kanji=null→空文字正規化├── types/│   └── index.ts               ← 【v1.1修正】型追加・mapFormatToWeakType└── scripts/    └── check-kanjivg.ts       ← 【v1.1追加】KanjiVGカバレッジ確認スクリプト1.2 実装前に決定する3点【v1.1修正】ChatGPT 指摘：未決定事項でなく実装方針として先に固定する決定事項採用方針理由問題取得方式逐次取得（for ループ）並列取得だと同一 history で発行され重複問題が混じるstudy/answer の整合性シリアル実行＋エラー補償（Supabase RPC は Phase 2 以降）Phase 1 は 1 ユーザーのみで競合なし。RPC 化は Phase 2 で対応Supabase クライアントanon/SSR 用・admin 用を完全分離service role を SSR クライアントに渡すと RLS が無効化される2. 型定義（types/index.ts）2.1 DB エンティティ型（変更なし）※ Subject / Format / InputType / Source / WeakType / CharacterStage / Question / StudyLog / WeakItem / GameProgress / Learner の型は v1.0 から変更なし。2.2 APIリクエスト・レスポンス型（v1.1修正）■ AnswerResponse に prev_stage を追加【v1.1修正】演出で『どこから進化したか』を返すために prev_stage を追加export type AnswerResponse = {  xp_earned:               number;  coins_earned:            number;  total_xp:                number;  level:                   number;  level_up:                boolean;  prev_level:              number;  character_stage:         CharacterStage;  character_stage_changed: boolean;  prev_stage:              CharacterStage;   // ← 【v1.1追加】  new_badges:              string[];  streak_days:             number;  next_level_xp:           number;};■ QuizSessionState に submittedIds を追加【v1.1修正】fetchInitialQuestions の逐次取得で重複防止するため submittedIds を追加export type QuizSessionState = {  sessionId:           string;  currentMode:         Mode;  questions:           Question[];  currentIndex:        number;  answers:             AnswerResult[];  consecutiveCorrect:  number;  isDailyFirst:        boolean;  isLoading:           boolean;  fetchError:          string | null;  submittedIds:        string[];  // ← 【v1.1追加】取得済み問題IDで重複防止};■ mapFormatToWeakType ヘルパー【v1.1修正】format as WeakType の危険な cast を廃止し変換関数を追加export function mapFormatToWeakType(format: Format, subject: Subject): WeakType {  const map: Record&lt;string, WeakType&gt; = {    reading:     &apos;reading&apos;,    writing:     &apos;writing&apos;,    stroke:      &apos;stroke&apos;,    choice:      &apos;meaning&apos;,    calculation: &apos;calculation&apos;,    sentence:    &apos;calculation&apos;,  };  return map[format] ?? (subject === &apos;japanese&apos; ? &apos;reading&apos; : &apos;calculation&apos;);}2.3 Zod バリデーション方針【v1.1修正】ChatGPT・Gemini 指摘：I/O 境界に Zod を導入して any を排除するClaude API レスポンスと Supabase からの外部データを Zod でパースするコンポーネント間の props は TypeScript の静的型のみで十分（Zod 不要）// lib/schemas.ts （新規追加）import { z } from &apos;zod&apos;;export const GeneratedQuestionSchema = z.object({  body:       z.string().min(1),  answer:     z.string().min(1),  hint:       z.string().nullable().optional(),  unit:       z.string().min(1),  grade:      z.number().int().min(1).max(6),  input_type: z.enum([&apos;voice&apos;,&apos;photo&apos;,&apos;keypad&apos;,&apos;choice&apos;,&apos;stroke&apos;]),  kanji:      z.string().nullable().optional(),});export type GeneratedQuestion = z.infer&lt;typeof GeneratedQuestionSchema&gt;;3. lib/ モジュール詳細設計3.1 lib/supabase.ts（v1.1修正：3クライアント分離）【v1.1修正】createServerSupabase に service role を渡していたバグを修正。3つのクライアントに分離import { createBrowserClient, createServerClient } from &apos;@supabase/ssr&apos;;import { createClient as createSbClient } from &apos;@supabase/supabase-js&apos;;import { cookies } from &apos;next/headers&apos;;// 1) ブラウザ（クライアントコンポーネント）用 anon クライアントexport function createClient() {  return createBrowserClient(    process.env.NEXT_PUBLIC_SUPABASE_URL!,    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!  );}// 2) API Routes / Server Components 用 SSR クライアント（anon key + cookie）export async function createServerSb() {  const cookieStore = await cookies();  return createServerClient(    process.env.NEXT_PUBLIC_SUPABASE_URL!,    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,    { cookies: { get: (name) =&gt; cookieStore.get(name)?.value } }  );}// 3) 管理操作（Phase 1: learner 固定解決）専用 admin クライアント//    RLS をバイパスするため、サーバーサイドでしか使ってはならないexport function createAdminSb() {  return createSbClient(    process.env.NEXT_PUBLIC_SUPABASE_URL!,    process.env.SUPABASE_SERVICE_ROLE_KEY!,    { auth: { persistSession: false, autoRefreshToken: false } }  );}// Phase 1: admin クライアントで learner_id を固定解決export async function getLearnerId(): Promise&lt;string&gt; {  const sb = createAdminSb();  const { data, error } = await sb    .from(&apos;learners&apos;).select(&apos;id&apos;).limit(1).maybeSingle();  if (error) throw new Error(`db_error: ${error.message}`);  if (!data) throw new Error(&apos;learner_not_found&apos;);  // 404 を呼び出し側で返す  return data.id;}3.2 lib/claude.ts（v1.1修正）【v1.1修正】JSON解析を {} 範囲切り出しで堅牢化。validateQuestion のチェックを強化// JSON 解析：説明文が混じっても { } 範囲を切り出すfunction extractJson(text: string): unknown {  const start = text.indexOf(&apos;{&apos;);  const end   = text.lastIndexOf(&apos;}&apos;);  if (start === -1 || end === -1) throw new Error(&apos;no_json_found&apos;);  return JSON.parse(text.slice(start, end + 1));}// バリデーション強化版function validateQuestion(raw: unknown, params: GenerateParams): GeneratedQuestion {  // Zod でパース（型安全・必須フィールド確認）  const parsed = GeneratedQuestionSchema.parse(raw);  // 読み問題：answer がひらがなのみか確認  if (params.format === &apos;reading&apos; &amp;&amp; !/^[ぁ-んー]+$/.test(parsed.answer)) {    throw new Error(&apos;answer_not_hiragana&apos;);  }  // 読み問題：body に answer が含まれていないか確認  if (params.format === &apos;reading&apos; &amp;&amp; parsed.body.includes(parsed.answer)) {    throw new Error(&apos;answer_in_body&apos;);  }  // input_type と format の組み合わせチェック  const validCombos: Record&lt;string, InputType&gt; = {    reading: &apos;voice&apos;, writing: &apos;photo&apos;, stroke: &apos;stroke&apos;,    choice: &apos;choice&apos;, calculation: &apos;keypad&apos;, sentence: &apos;keypad&apos;,  };  if (parsed.input_type !== validCombos[params.format]) {    throw new Error(`invalid_input_type_combo: ${params.format}/${parsed.input_type}`);  }  return parsed;}3.3 lib/kanjivg.ts（v1.1修正：DOMParser 化）【v1.1修正】regex パースを廃止し DOMParser で XML として解析。不要な補助要素を除外export async function loadKanjiStrokes(kanji: string): Promise&lt;string[]&gt; {  const code = kanji.codePointAt(0)!.toString(16).padStart(5, &apos;0&apos;);  const res  = await fetch(`/kanjivg/${code}.svg`);  if (!res.ok) throw new Error(`kanjivg_not_found:${kanji}`);  const svgText = await res.text();  // DOMParser で XML 解析（ブラウザ環境）  const parser = new DOMParser();  const doc    = parser.parseFromString(svgText, &apos;image/svg+xml&apos;);  // kvg:element 等の補助要素を除き、実際の stroke path のみを取得  // KanjiVG の stroke path は id が &apos;kvg:...&apos; で始まるグループに属する  const paths = Array.from(doc.querySelectorAll(&apos;path&apos;))    .filter(el =&gt; !el.closest(&apos;[id^=&quot;kvg:StrokeNumbers&quot;]&apos;))  // 画数数字SVGを除外    .map(el =&gt; el.getAttribute(&apos;d&apos;) ?? &apos;&apos;)    .filter(d =&gt; d.length &gt; 0);  if (paths.length === 0) throw new Error(`no_strokes_found:${kanji}`);  return paths;}// 正解・不正解パターン生成（変更なし）export function correctOrder(paths: string[]): string[] { return [...paths]; }export function wrongOrder(paths: string[]): string[] {  if (paths.length &lt; 2) return [...paths];  const wrong = [...paths];  const i = Math.floor(Math.random() * (paths.length - 1));  [wrong[i], wrong[i+1]] = [wrong[i+1], wrong[i]];  return wrong;}3.4 lib/kanjivg-node.ts（v1.1追加：Node.js 専用スクリプト）【v1.1修正】checkCoverage を client/server 共通ファイルから分離し、Node 専用スクリプトとして独立// scripts/check-kanjivg.ts として呼び出すimport fs from &apos;node:fs&apos;;import path from &apos;node:path&apos;;export function checkCoverage(kanjiList: string[], svgDir: string): string[] {  return kanjiList.filter(k =&gt; {    const code = k.codePointAt(0)!.toString(16).padStart(5, &apos;0&apos;);    return !fs.existsSync(path.join(svgDir, `${code}.svg`));  });}// 実行方法：npx tsx scripts/check-kanjivg.ts// 出力例：不足漢字リスト.txt を生成3.5 lib/speech.ts（v1.1修正：errMap 完全化）【v1.1修正】audio-capture / service-not-allowed など未知のエラーコードを追加。unknown ケースは timeout でなく unknown に落とすconst errMap: Record&lt;string, SpeechError&gt; = {  &apos;not-allowed&apos;:      &apos;permission_denied&apos;,  &apos;permission-denied&apos;:&apos;permission_denied&apos;,  &apos;no-speech&apos;:        &apos;no_speech&apos;,  &apos;aborted&apos;:          &apos;no_speech&apos;,  &apos;network&apos;:          &apos;network_error&apos;,  &apos;audio-capture&apos;:    &apos;network_error&apos;,   // ← 追加  &apos;service-not-allowed&apos;: &apos;permission_denied&apos;,  // ← 追加};rec.onerror = (e) =&gt; {  clearTimeout(timer);  reject(errMap[e.error] ?? &apos;no_speech&apos;);  // 未知は no_speech（再試行を促す）};// SpeechRecognition start 連打防止・unmount 時 abortexport function useSpeechRecognition() {  const recRef = useRef&lt;SpeechRecognition | null&gt;(null);  useEffect(() =&gt; {    return () =&gt; { recRef.current?.abort(); };  // unmount 時に必ず abort  }, []);  // ... recognizeSpeech は recRef 経由で1インスタンスのみ管理}3.6 lib/xp.ts（v1.1修正：calcNextLevelXp バグ修正）【v1.1修正】xpTotal=80, level=2 のとき 160 を返すバグを修正// 修正前（バグあり）// const nextLevelThreshold = level * 80;// return nextLevelThreshold - (xpTotal % 80);  // level=2, xpTotal=80 → 160-0=160 （間違い）// 修正後export function calcNextLevelXp(level: number, xpTotal: number): number {  const nextThreshold = level * 80;   // 次レベルに必要な累計XP  return Math.max(nextThreshold - xpTotal, 0);}【v1.1修正】consecutive_correct が『回答前の連続正解数（preAnswerStreak）』であることを明示// calcXp の consecutive_correct は回答前の値（preAnswerStreak）を渡す// 今回の正解を含めた判定は calcXp 内で +1 して行うexport function calcXp(input: XpInput): number {  if (!input.is_correct) return 0;  let xp = 10;  if (input.priority_score &gt; 0.6) xp += 8;  if (input.input_type === &apos;voice&apos;)  xp += 2;  if (input.input_type === &apos;stroke&apos;) xp += 2;  // preAnswerStreak + 1 で今回を含めた連続正解数とする  const streak = input.consecutive_correct + 1;  // ← 今回分を +1  if (streak &gt;= 3) xp += Math.min((streak - 2) * 5, 15);  if (input.is_daily_first) xp += 20;  if (input.used_hint)      xp = Math.floor(xp / 2);  return xp;}3.7 lib/priority.ts（v1.1修正：kanji=null 空文字正規化）【v1.1修正】kanji が null の場合 PostgreSQL の UNIQUE 制約が重複を許すため空文字に正規化export async function upsertWeakItem(  supabase: ReturnType&lt;typeof createAdminSb&gt;,  learnerId: string,  question: Question,  isCorrect: boolean): Promise&lt;void&gt; {  // kanji=null → &apos;&apos; に正規化（NULL は UNIQUE 制約で重複を許すため）  const kanji = (question.subject === &apos;japanese&apos; &amp;&amp; question.subject)    ? (question.answer ?? &apos;&apos;)   // 読み問題は answer が漢字読みなので kanji フィールドから取得    : &apos;&apos;;  const key = {    learner_id: learnerId,    subject:    question.subject,    unit:       question.unit,    grade:      question.grade,    weak_type:  mapFormatToWeakType(question.format, question.subject),    kanji,      // &apos;&apos; or 実際の漢字文字列  };  // ...（以降は v1.0 と同様）}4. API Routes 詳細設計4.1 GET /api/questions/next/route.ts（v1.1修正）【v1.1修正】.not().single() を .not().maybeSingle() に変更。not-in の UUID quoting を安全化// キャッシュ検索（修正版）const safeHistory = history.length &gt; 0  ? history.map(id =&gt; `&quot;${id}&quot;`).join(&apos;,&apos;)  : null;let query = sb.from(&apos;questions&apos;)  .select(&apos;*&apos;)  .eq(&apos;query_key&apos;, queryKey)  .eq(&apos;source&apos;, &apos;api_generated&apos;)  .or(&apos;avg_correct.is.null,avg_correct.gte.0.3&apos;)  .order(&apos;use_count&apos;, { ascending: true })  .limit(1);if (safeHistory) {  query = query.not(&apos;id&apos;, &apos;in&apos;, `(${safeHistory})`);}// .single() → .maybeSingle() に変更（0件時にエラーではなく null を返す）const { data: cached, error: cacheErr } = await query.maybeSingle();if (cacheErr) {  console.error(&apos;cache_query_error&apos;, cacheErr);  // エラーでも処理継続（生成にフォールバック）}【v1.1修正】フォールバック時も subject + format + input_type を合わせた問題を返す// フォールバック（改善版）const { data: fallback } = await sb.from(&apos;questions&apos;)  .select(&apos;*&apos;)  .eq(&apos;subject&apos;, subject)  .eq(&apos;format&apos;, format)         // ← format も合わせる  .eq(&apos;input_type&apos;, input_type)  // ← input_type も合わせる  .order(&apos;use_count&apos;, { ascending: true })  .limit(1)  .maybeSingle();【v1.1修正】rate limit 推奨を注記※ Phase 1 は認証なし。Claude API を呼び出す endpoint（questions/next）は Vercel の IP rate limit または Upstash Redis による簡易 rate limit の追加を推奨する。4.2 POST /api/study/answer/route.ts（v1.1修正）【v1.1修正】Promise.all での並列更新をシリアル実行に変更。途中失敗時のエラー補償を追加【v1.1修正】payload を const payload: AnswerResponse として明示export async function POST(req: NextRequest) {  const body: AnswerRequest = await req.json();  const sb        = createAdminSb();   // ← admin クライアントを使用  const learnerId = await getLearnerId();  // 問題・ゲーム進捗の取得  const { data: question, error: qErr } = await sb    .from(&apos;questions&apos;).select(&apos;*&apos;).eq(&apos;id&apos;, body.question_id).maybeSingle();  if (qErr || !question) {    return NextResponse.json({ error: &apos;invalid_question_id&apos; }, { status: 404 });  }  const { data: gp, error: gpErr } = await sb    .from(&apos;game_progress&apos;).select(&apos;*&apos;).eq(&apos;learner_id&apos;, learnerId).maybeSingle();  if (gpErr || !gp) {    return NextResponse.json({ error: &apos;game_progress_not_found&apos; }, { status: 404 });  }  // XP・レベル等の計算（v1.0 と同様）  const today       = new Date().toISOString().slice(0, 10);  const isDailyFirst = gp.last_study_date !== today;  const xpEarned    = calcXp({ /* ... preAnswerStreak を渡す */ });  const coinsEarned = calcCoins(xpEarned);  const newXp       = gp.xp_total + xpEarned;  const prevLevel   = gp.level;  const prevStage   = gp.character_stage;  const newLevel    = calcLevel(newXp);  const newStage    = calcCharacterStage(newLevel);  const newStreak   = updateStreak(gp.last_study_date, today, gp.streak_days);  const newBadges   = checkBadges({ /* ... */ });  // シリアル実行（途中失敗時はエラーを返すが後続はスキップ）  const errors: string[] = [];  const { error: logErr } = await sb.from(&apos;study_logs&apos;).insert({ /* ... */ });  if (logErr) errors.push(`study_logs: ${logErr.message}`);  const { error: gpUpdateErr } = await sb.from(&apos;game_progress&apos;).update({    xp_total: newXp, level: newLevel, character_stage: newStage,    coins: gp.coins + coinsEarned, streak_days: newStreak,    last_study_date: today, badges: [...gp.badges, ...newBadges],    updated_at: new Date().toISOString(),  }).eq(&apos;learner_id&apos;, learnerId);  if (gpUpdateErr) errors.push(`game_progress: ${gpUpdateErr.message}`);  await upsertWeakItem(sb, learnerId, question, body.is_correct)    .catch(e =&gt; errors.push(`weak_items: ${e.message}`));  await updateQuestionAvgCorrect(sb, body.question_id, body.is_correct)    .catch(e =&gt; errors.push(`questions: ${e.message}`));  if (errors.length &gt; 0) {    console.error(&apos;study_answer_partial_error&apos;, errors);    // XP 付与が成功していれば 200 で返す（ゲーム体験を優先）    // GP 更新失敗時のみ 500 を返す    if (errors.some(e =&gt; e.startsWith(&apos;game_progress&apos;))) {      return NextResponse.json({ error: &apos;game_progress_update_failed&apos; }, { status: 500 });    }  }  // payload を明示的に型付け  const payload: AnswerResponse = {    xp_earned: xpEarned, coins_earned: coinsEarned,    total_xp: newXp, level: newLevel,    level_up: newLevel &gt; prevLevel, prev_level: prevLevel,    character_stage: newStage, character_stage_changed: newStage !== prevStage,    prev_stage: prevStage,   // ← v1.1 追加    new_badges: newBadges, streak_days: newStreak,    next_level_xp: calcNextLevelXp(newLevel, newXp),  };  return NextResponse.json(payload);}5. コンポーネント詳細設計5.1 QuizSession.tsx（v1.1修正：逐次取得・res.ok チェック）【v1.1修正】fetchInitialQuestions を for ループの逐次取得に変更して重複問題を防止async function fetchInitialQuestions() {  try {    const picked: Question[] = [];    for (let i = 0; i &lt; 3; i++) {      const q = await fetchQuestion(picked.map(v =&gt; v.id));  // 取得済みIDを渡す      picked.push(q);    }    setState(s =&gt; ({ ...s, questions: picked, isLoading: false, submittedIds: [] }));  } catch {    setState(s =&gt; ({ ...s, isLoading: false, fetchError: &apos;問題の準備ができませんでした&apos; }));  }}// fetchQuestion は extraHistory を引数で受け取る（stale closure 回避）async function fetchQuestion(extraHistory: string[] = []): Promise&lt;Question&gt; {  const history = [    ...state.questions.map(q =&gt; q.id),    ...extraHistory,  ].join(&apos;,&apos;);  const res = await fetch(    `/api/questions/next?subject=${subject}&amp;format=${format}&amp;mode=${mode}&amp;session_history=${history}`  );  if (!res.ok) throw new Error(`fetch_failed: ${res.status}`);  // ← v1.1 追加  return res.json();}【v1.1修正】submitAnswer にも res.ok チェックを追加async function submitAnswer(/* ... */): Promise&lt;AnswerResponse&gt; {  const res = await fetch(&apos;/api/study/answer&apos;, { method: &apos;POST&apos;, /* ... */ });  if (!res.ok) {    const errBody = await res.json().catch(() =&gt; ({}));    throw new Error(`answer_submit_failed: ${res.status} ${errBody.error ?? &apos;&apos;}`);  }  return res.json();}5.2 StrokeAnimation.tsx（v1.1修正：正解 Index バグ修正）【v1.1修正】A が常に正解パターンになっていたバグを修正。correct_index を問題データとして持たせる// question.options = [&apos;A&apos;, &apos;B&apos;] で、correct_index が正解の index（0 or 1）// API 側で問題生成時に 0/1 をランダムに割り当てるconst correctIndex = question.correct_index ?? 0;  // 0: A が正解, 1: B が正解// A・B それぞれに割り当てるパターンconst pathsA = correctIndex === 0 ? correctOrder(strokes) : wrongOrder(strokes);const pathsB = correctIndex === 1 ? correctOrder(strokes) : wrongOrder(strokes);async function handleChoice(choice: &apos;A&apos; | &apos;B&apos;) {  if (answered) return;  setAnswered(true);  const isCorrect = (choice === &apos;A&apos; &amp;&amp; correctIndex === 0)                 || (choice === &apos;B&apos; &amp;&amp; correctIndex === 1);  // 正解アニメーション再生  setIsReplaying(true);  await delay(1500);  setIsReplaying(false);  await onAnswer(isCorrect, 0, false, choice);}【v1.1修正】Gemini 指摘：SVG アニメーションと React 再レンダリングの競合対策。SVG 要素は useRef で直接操作// StrokeSVG コンポーネント内で useRef を使い、CSS animation を直接制御// framer-motion は依存を増やすため不採用。CSS stroke-dashoffset で実装するconst svgRef = useRef&lt;SVGSVGElement&gt;(null);useEffect(() =&gt; {  if (!svgRef.current || !paths.length) return;  // useEffect 内で直接 SVG DOM を操作してアニメーションを制御  animateStrokes(svgRef.current, paths);}, [paths]);  // paths が変わった時のみ再実行5.3 VoiceInput.tsx（v1.1修正：timeout 状態の追加）【v1.1修正】VoiceState union に timeout が漏れていたため追加。errMap の timeout → timeout に対応// lib/speech.ts の SpeechError 型export type SpeechError =  | &apos;not_supported&apos; | &apos;permission_denied&apos; | &apos;no_speech&apos;  | &apos;network_error&apos; | &apos;timeout&apos;;   // ← timeout を明示// VoiceInput の errMapconst errMap: Record&lt;SpeechError, VoiceState&gt; = {  &apos;not_supported&apos;:     &apos;choice_fallback&apos;,  &apos;permission_denied&apos;: &apos;permission_denied&apos;,  &apos;no_speech&apos;:         &apos;no_speech&apos;,  &apos;network_error&apos;:     &apos;network_error&apos;,  &apos;timeout&apos;:           &apos;no_speech&apos;,    // timeout は no_speech と同様に扱う};6. エラーハンドリング詳細設計6.1 App Router 標準エラーUI（v1.1追加）【v1.1修正】learn/error.tsx / parent/error.tsx / loading.tsx を追加// app/learn/error.tsx&apos;use client&apos;;export default function LearnError({  error, reset,}: { error: Error &amp; { digest?: string }; reset: () =&gt; void }) {  return (    &lt;div className=&apos;flex flex-col items-center justify-center min-h-screen gap-6 p-8 bg-gray-50&apos;&gt;      &lt;p className=&apos;text-5xl&apos;&gt;😥&lt;/p&gt;      &lt;p className=&apos;text-2xl font-bold text-gray-800&apos;&gt;ちょっと問題が起きちゃった&lt;/p&gt;      &lt;p className=&apos;text-base text-gray-500&apos;&gt;もう1回試してみてね&lt;/p&gt;      &lt;button        onClick={reset}        className=&apos;bg-indigo-500 text-white rounded-2xl px-8 py-4 text-xl font-bold&apos;      &gt;        もう1回試す      &lt;/button&gt;      &lt;a href=&apos;/learn&apos; className=&apos;text-indigo-400 underline text-base&apos;&gt;ホームへもどる&lt;/a&gt;    &lt;/div&gt;  );}// app/learn/loading.tsxexport default function LearnLoading() {  return (    &lt;div className=&apos;flex items-center justify-center min-h-screen&apos;&gt;      &lt;div className=&apos;animate-spin text-6xl&apos;&gt;⭐&lt;/div&gt;    &lt;/div&gt;  );}// app/parent/error.tsx・loading.tsx も同様のパターンで作成6.2 エラーコード体系（v1.1修正：業務エラーとシステムエラーを分離）【v1.1修正】rate_limited / invalid_question_id / invalid_mode を追加分類エラーコードHTTP説明業務learner_not_found404学習者未登録（初回セットアップ前）業務invalid_question_id404存在しない問題ID業務game_progress_not_found404game_progress 未初期化業務invalid_mode400mode パラメータが不正業務rate_limited429API 呼び出し頻度超過システムdb_error500Supabase DB エラーシステムclaude_generation_failed502Claude API 2回リトライ後も失敗システムgame_progress_update_failed500XP/レベル更新失敗システムno_questions503キャッシュも生成も失敗7. テスト方針7.1 追加テストケース（v1.1修正）【v1.1修正】ChatGPT 指摘の不足テストケースを追加対象テストケース優先度calcNextLevelXpxpTotal=80, level=2 → 80 を返す（修正確認）高calcNextLevelXpxpTotal=79, level=1 → 1 を返す（境界値）高calcNextLevelXpxpTotal=160, level=2 → 0 を返す（ジャスト）高upsertWeakItemkanji=null で2回 upsert しても重複行が生まれない高submitAnswerres.status=500 で Error がスローされる中fetchInitialQuestions3問が全て異なる question_id で取得される中calcXpconsecutive_correct=2（preAnswerStreak）で streak=3 になりボーナス付与中validateQuestionmath 問題に voice が返った場合に例外をスロー中extractJson前後に説明文が混じった文字列からJSON部分のみ抽出できる中8. 環境構築手順8.1 セットアップ手順（v1.1修正：詳細化）Stepコマンド / 操作確認方法①npx create-next-app@latest my-study-app --typescript --tailwind --appnpm run dev が起動する②npm install @supabase/supabase-js @supabase/ssr @anthropic-ai/sdk zod recharts canvas-confetti p-limit uuidnode_modules に存在する③npm install -D tsx @types/uuid vitest @testing-library/react型エラーなし④.env.local に環境変数を設定（下記参照）各変数が undefined でない⑤Supabase SQL Editor で CREATE TABLE を実行4テーブルが存在する⑥seed.sql を実行（idempotent版・下記参照）learners と game_progress に1レコード存在する⑦npx tsx scripts/check-kanjivg.ts を実行不足漢字リストを確認し SVG を配置⑧types/index.ts → lib/* → api/* → components/* の順に実装各ユニットテストが通過⑨npm run dev で動作確認（localhost:3000）ホーム画面が表示される⑩iOS Safari 実機テスト音声・書き順・全画面が正常動作8.2 .env.local 設定値NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.coNEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...（anon key）SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...（service role key ★サーバーのみ）ANTHROPIC_API_KEY=sk-ant-...8.3 初期データ seed.sql（v1.1修正：idempotent化）【v1.1修正】再実行可能な idempotent seed に変更-- learner 初期データ（子どもの名前・学年は実際の値に変更）INSERT INTO learners (name, grade, avatar_name)VALUES (&apos;たろう&apos;, 4, &apos;たまご&apos;)ON CONFLICT DO NOTHING;   -- ← 再実行しても重複しない-- game_progress の初期レコードINSERT INTO game_progress (learner_id)  SELECT id FROM learners ORDER BY created_at LIMIT 1ON CONFLICT (learner_id) DO NOTHING;   -- ← 再実行しても重複しない9. 後続フェーズへの申し送り項目Phase概要Supabase RPC によるトランザクション2study/answer の複数テーブル更新を DB 側の RPC で atomic に実行Supabase RLS ポリシー強化2Phase 2 PIN 認証導入と同時に learner_id ベースに変更プリント取り込みAPI2POST /api/print/import 実装保護者 PIN 認証2/parent/* に middleware で PIN 適用書き取り判定API3POST /api/writing/judge 実装書き順上級モード4KanjiVG ドラッグ並び替えUIPWA 化4manifest + Service WorkerChrome Android テスト追加4Web Speech API の Android 互換確認Vercel rate limit1後半IP 単位の簡易 rate limit を questions/next に追加※ 本書は詳細設計書 v1.0 に ChatGPT・Gemini レビュー（計20件）を全件反映した v1.1 です。8.1節のステップ順に Claude Code へ実装指示を行ってください。